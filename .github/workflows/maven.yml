# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}
          MYSQL_DATABASE: badk
          MYSQL_USER: Arne
          MYSQL_PASSWORD: ${{ secrets.JDBC_DATABASE_PASSWORD }}
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven
        env:
          JDBC_DATABASE_URL: ${{ secrets.JDBC_DATABASE_URL }}
          JDBC_DATABASE_USER: ${{ secrets.JDBC_DATABASE_USER }}
          JDBC_DATABASE_PASSWORD: ${{ secrets.JDBC_DATABASE_PASSWORD }}
        run: mvn -B package --file pom.xml


#        JDBC_DATABASE_URL: jdbc:mysql://bilabonnementabd.mysql.database.azure.com:3306/badk?useSSL=true
#        JDBC_DATABASE_USER: Arne
#        JDBC_DATABASE_PASSWORD: ${JDBC_DATABASE_PASSWORD}
#
#        JDBC_DATABASE_URL: ${{ secrets.JDBC_DATABASE_URL }}
#        JDBC_DATABASE_USER: ${{ secrets.JDBC_DATABASE_USER }}
#        JDBC_DATABASE_PASSWORD: ${{ secrets.JDBC_DATABASE_PASSWORD }}